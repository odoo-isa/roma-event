<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- FORM View  -->
    <record model='ir.ui.view' id='view_move_form_full_reconcile'>
        <field name='name'>view.move.form.full_reconcile</field>
        <field name='model'>account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name='arch' type='xml'>
            <xpath expr="//field[@name='line_ids']/tree/field[@name='tag_ids']" position="after">
                <field name="full_reconcile_id" optional="hide"/>
            </xpath>
        </field>
    </record>

</odoo>