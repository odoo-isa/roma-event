<?xml version="1.0" encoding="utf-8"?>
<openerp>
    <data>

        <!-- FORM View  -->
        <record model='ir.ui.view' id='res_config_settings_view_form_threshold'>
            <field name='name'>res.config.settings.view.form.threshold</field>
            <field name='model'>res.config.settings</field>
            <field name="priority" eval="41"/>
            <field name="inherit_id" ref="account.res_config_settings_view_form"/>
            <field name='arch' type='xml'>
                <xpath expr="//div[@id='bank_cash']/div" position="after">
                    <div class="col-xs-12 col-md-6 o_setting_box">
                        <div class="o_setting_left_pane"/>
                        <div class="o_setting_right_pane">
                            <label for="bank_reconciliation_threshold"/>
                            <div class="text-muted">
                                Will be raise a warning message if will be exceed this value during payment matching.
                                This check will be performed only if there is deficit between credit and debit and if
                                the writeoff account has been specified.
                            </div>
                            <field name="bank_reconciliation_threshold"/>
                        </div>
                    </div>
                </xpath>
            </field>
        </record>

    </data>
</openerp>