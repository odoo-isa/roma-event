<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!-- TREE View  -->
        <record model='ir.ui.view' id='view_sale_order_raee_tree'>
            <field name='name'>view.sale.order.raee.tree</field>
            <field name='model'>sale.order</field>
            <field name="inherit_id" ref="sale.view_order_form"/>
            <field name='arch' type='xml'>
                <xpath expr="//field[@name='order_line']//tree" position="attributes">
                    <attribute name="default_order">id</attribute>
                </xpath>
                <xpath expr="//field[@name='order_line']/form" position="after">
                    <field name="link_with_raee" invisible="1"/>
                </xpath>
            </field>
        </record>

    </data>
</odoo>